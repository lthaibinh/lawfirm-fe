export const DASHBOARD_PATH = '/admin/dashboard';

interface Config {
  fontFamily: string;
  borderRadius: number;
}

const config: Config = {
  fontFamily: `'Roboto', sans-serif`,
  borderRadius: 8
};

export default config;
